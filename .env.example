# Clé API Anthropic pour la couche d'explication en langage naturel.
# Sans cette clé, Sentinel fonctionne quand même (fallback texte simple),
# mais la Fonction 3 "AI Assistant" perd son côté conversationnel réel.
ANTHROPIC_API_KEY=

PORT=3000
